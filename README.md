# Multi-Environment-infra-aws
A multi-environment infrastructure using Terraform is a setup where the same reusable module (for resources like EC2, S3, and DynamoDB) is deployed multiple times with different configurations for environments such as dev, staging, and production
